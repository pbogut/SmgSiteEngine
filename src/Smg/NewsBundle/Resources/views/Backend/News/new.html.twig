{% extends 'SmgNewsBundle::backend.html.twig' %}
{% block content %}
<form action="{{ path('backend_news_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_news') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}