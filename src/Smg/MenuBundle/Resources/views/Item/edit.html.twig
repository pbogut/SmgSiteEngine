{% block content %}
<h1>Edycja elementu</h1>

<form action="{{ path('item_update', { 'id': item.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-primary" type="submit">Zapisz</button>
    </p>
</form>
{% endblock %}